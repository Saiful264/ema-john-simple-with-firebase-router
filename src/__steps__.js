/**
 * 1. create a firebase project
 * 2. create a web application
 * 3. npm i firebase
 * 4. save firebse config and export app 
 * 5. Build > authentication > get started > enable sign in method
 * 6. creat sing up and login
 * */ 

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the childrem props
 * 4. set context value
 * 5. set the provider
 * */ 

/**
 * HOSTING
 * ---------------
 * 1. one time per computer or laptop
 * 2. npm install -g firebase-tools
 * 3.firebase login
 * 
 * 
 * 
 * 
 * For each project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow) use space bar to select
 * 4. existing project 
 * 5. select the project carefully
 * 6. What do you want to use as your public directory? vite : dist, create react app : 
 * 7. Configure as a single-page app (rewrite all urls to /index.html)? Yes
 * 8.  Set up automatic builds and deploys with GitHub? No
 * 
 * 
 * 
 * For every time 
 * 1. npm run build
 * 2. firebase diploy
 * */ 